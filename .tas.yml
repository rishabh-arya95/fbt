---
framework: jest
preRun:
     command:
        - yarn install --immutable
preMerge:
  pattern:
    - "**/__tests__/**/*-test.js"
postMerge:
  pattern: 
    - "**/__tests__/**/*-test.js"
postRun:
     command:
        - yarn --version
        - sleep 1000
version: 1.0
