---
framework: jest
preRun:
     command:
        - yarn install --immutable
preMerge:
  pattern:
    - "**/__tests__/**/*-test.js"
postMerge:
  pattern: 
    - "demo-app/src/example/__tests__/Example.react-test.js"
postRun:
     command:
        - yarn --version
version: 1.0
